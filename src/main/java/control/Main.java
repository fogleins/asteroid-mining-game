package control;

import view.StartWindow;

public class Main {

    // opens the first window to start the program
    public static void main(String[] args) {
        // Displays the start screen.
        StartWindow sw = new StartWindow();
        sw.showFrame();
    }
}
